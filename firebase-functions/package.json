{
  "name": "qwen-proxy-function",
  "description": "Qwen-VL Max API 代理 Firebase Function",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "lint": "echo 'No linting configured'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-functions": "^4.5.0",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0",
    "stripe": "^14.11.0"
  }
}
