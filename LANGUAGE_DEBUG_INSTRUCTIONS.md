# 語言切換調試指南

## 🔍 問題現象

**症狀**：
- 點擊語言切換
- 控制台顯示：`🌐 語言已切換: English`
- 但頁面內容仍然是中文 ❌

---

## 📋 調試步驟

### 第1步：清除緩存
**非常重要！必須先清除緩存**

```bash
Mac: Cmd + Shift + R
Windows: Ctrl + Shift + R
```

### 第2步：打開控制台
1. 右鍵點擊頁面 → 檢查（Inspect）
2. 切換到 Console 標籤
3. 確保沒有過濾器

### 第3步：訪問頁面
```
https://vaultcaddy.com/index.html
```

### 第4步：點擊語言切換
點擊「繁體中文 ▼」，選擇「English」

### 第5步：查看控制台輸出
**應該看到以下日誌：**

```
🌐 切換語言: zh → en
🔵 準備調用 translatePage()...
🔄 ============ 開始翻譯頁面 ============
📍 當前語言: en
📍 Translations 對象: XXX 個 keys
📝 找到 XXX 個需要翻譯的元素
🔍 [0] Key: "hero.trust"
   原文: "超過 200+ 企業信賴"
   譯文: "Trusted by 200+ Businesses"
🔍 [1] Key: "hero.title1"
   原文: "針對香港銀行對帳單處理"
   譯文: "Targeted at Hong Kong Bank Statement Processing"
...
✅ 頁面翻譯完成 - 成功: XXX, 失敗: 0
🔵 translatePage() 調用完成
```

---

## ❌ 可能的問題

### 問題 1：沒看到翻譯日誌
**症狀**：
```
🌐 切換語言: zh → en
（沒有其他日誌）
```

**原因**：
- `translatePage()` 沒有被調用
- 或者控制台過濾了日誌

**解決方案**：
1. 檢查控制台過濾器（確保顯示所有級別：Verbose, Info, Warnings, Errors）
2. 手動執行：`window.languageManager.translatePage()`

### 問題 2：翻譯日誌顯示但頁面沒變
**症狀**：
```
✅ 頁面翻譯完成 - 成功: 50, 失敗: 0
（但頁面內容仍是中文）
```

**原因**：
- 元素缺少 `data-i18n` 屬性
- 翻譯字典中沒有對應的 key
- 頁面在翻譯後被重新渲染

**解決方案**：
1. 檢查元素是否有 `data-i18n` 屬性
2. 檢查翻譯字典是否有該 key
3. 檢查是否有其他 JavaScript 重新渲染了頁面

### 問題 3：部分翻譯成功，部分失敗
**症狀**：
```
✅ 頁面翻譯完成 - 成功: 30, 失敗: 20
⚠️ 翻譯失敗: hero.subtitle
⚠️ 翻譯失敗: features.title
```

**原因**：
- 某些 key 在翻譯字典中不存在

**解決方案**：
1. 檢查控制台中失敗的 key
2. 在 `language-manager.js` 中添加缺失的翻譯

---

## 🧪 手動測試

### 在控制台中執行以下命令：

#### 1. 檢查 languageManager 是否存在
```javascript
window.languageManager
// 應該輸出：LanguageManager {currentLanguage: "zh", translations: {...}}
```

#### 2. 獲取當前語言
```javascript
window.languageManager.getCurrentLanguage()
// 應該輸出："zh" 或 "en"
```

#### 3. 手動切換語言
```javascript
window.languageManager.setLanguage('en')
// 應該看到翻譯日誌
```

#### 4. 手動觸發翻譯
```javascript
window.languageManager.translatePage()
// 應該看到翻譯日誌
```

#### 5. 檢查特定元素的翻譯
```javascript
// 檢查元素是否有 data-i18n 屬性
document.querySelector('[data-i18n="hero.trust"]')
// 應該輸出：<span data-i18n="hero.trust">...</span>

// 檢查翻譯是否存在
window.languageManager.translate('hero.trust')
// 應該輸出："Trusted by 200+ Businesses"
```

#### 6. 查找所有帶 data-i18n 的元素
```javascript
document.querySelectorAll('[data-i18n]').length
// 應該輸出：大於 50 的數字
```

---

## 📊 預期結果

### 成功的翻譯（應該看到）：
| 中文 | 英文 |
|------|------|
| 超過 200+ 企業信賴 | Trusted by 200+ Businesses |
| 針對香港銀行對帳單處理 | Targeted at Hong Kong Bank Statement Processing |
| 低至 | As low as |
| 專為會計師及小型公司設計的 AI 文檔處理平台 | AI document processing platform designed for accountants and small businesses |
| 免費試用 20 頁 | Free Trial 20 Pages |
| 了解收費 | Learn Pricing |
| 平均處理時間 | Average Processing Time |
| 數據準確率 | Data Accuracy |
| 企業客戶 | Business Clients |

### 頁面內容應該完全變成英文 ✅

---

## 🔧 如果問題仍然存在

### 方法 1：檢查緩存
```javascript
// 檢查 localStorage 中的語言設置
localStorage.getItem('preferred-language')
// 應該輸出："en"

// 清除並重新設置
localStorage.removeItem('preferred-language')
window.languageManager.setLanguage('en')
```

### 方法 2：檢查翻譯字典
```javascript
// 查看所有翻譯 keys
Object.keys(window.languageManager.translations)
// 應該輸出：["nav.features", "nav.pricing", ...]
```

### 方法 3：強制重新加載
```javascript
// 強制刷新頁面
location.reload(true)
```

---

## 📸 請提供以下截圖

如果問題仍然存在，請提供：

1. **控制台完整輸出**（從點擊語言切換到翻譯完成）
2. **頁面當前狀態**（顯示中文內容的部分）
3. **元素檢查**（右鍵點擊未翻譯的文字 → 檢查，查看是否有 `data-i18n` 屬性）

---

**更新時間**：2025年11月26日  
**版本**：1.1（增強調試）

