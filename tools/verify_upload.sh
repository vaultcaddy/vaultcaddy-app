#!/bin/bash

# 验证上传是否成功的脚本

echo "╔════════════════════════════════════════════════════════════════════╗"
echo "║         🔍 验证文件上传状态                                       ║"
echo "╚════════════════════════════════════════════════════════════════════╝"
echo ""

# 检查JS文件
echo "【1/5】检查 document-detail-new.js..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
curl -s -I "https://vaultcaddy.com/document-detail-new.js" | grep -E "HTTP|Content-Length" || echo "❌ 无法访问"
echo ""

# 检查中文版HTML
echo "【2/5】检查 document-detail.html..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
curl -s -I "https://vaultcaddy.com/document-detail.html" | grep -E "HTTP|Content-Length" || echo "❌ 无法访问"
echo ""

# 检查英文版HTML
echo "【3/5】检查 en/document-detail.html..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
curl -s -I "https://vaultcaddy.com/en/document-detail.html" | grep -E "HTTP|Content-Length" || echo "❌ 无法访问"
echo ""

# 检查日文版HTML
echo "【4/5】检查 jp/document-detail.html..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
curl -s -I "https://vaultcaddy.com/jp/document-detail.html" | grep -E "HTTP|Content-Length" || echo "❌ 无法访问"
echo ""

# 检查韩文版HTML
echo "【5/5】检查 kr/document-detail.html..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
curl -s -I "https://vaultcaddy.com/kr/document-detail.html" | grep -E "HTTP|Content-Length" || echo "❌ 无法访问"
echo ""

echo "════════════════════════════════════════════════════════════════════"
echo "      ✅ 如果所有文件都显示 HTTP/2 200，说明上传成功！"
echo "════════════════════════════════════════════════════════════════════"
echo ""
echo "下一步：清除浏览器缓存（Ctrl+Shift+R）并测试页面"
echo ""

