# 銀行對帳單導出功能指南

## 📊 功能概述

VaultCaddy 現在支持將銀行對帳單導出為多種會計軟件格式，包括：

1. **標準 CSV** - 完整的銀行對帳單格式（圖1參考）
2. **Xero CSV** - Xero 會計軟件專用格式（圖3參考）
3. **QuickBooks CSV** - QuickBooks 會計軟件專用格式（圖4參考）

---

## 🎯 導出格式對比

### 1. 標準 CSV 格式（圖1）

**適用於**:
- 需要完整銀行對帳單數據
- Excel 分析
- 自定義會計系統

**包含字段**:
```csv
CustomerName,AccountNumber,AccountType,BankName,BankAddress,PopulatedDate,EndDate,OpeningBalance,EndingBalance,LineItems_Date,LineItems_Description,LineItems_Credits,LineItems_Debits,LineItems_Balance
```

**示例**:
```csv
MR YEUNG CAVLIN,766-452064-882,Integrated Account,MIPS (likely bank code 024),EAST POINT CITY (766)...,06/01/2025,03/22/2025,121080.69,30188.66,02/22/2025,B/F BALANCE,0,0,1493.98
MR YEUNG CAVLIN,766-452064-882,Integrated Account,MIPS (likely bank code 024),EAST POINT CITY (766)...,06/01/2025,03/22/2025,121080.69,30188.66,02/26/2025,CREDIT INTEREST,2.61,0,1496.59
```

**特點**:
- ✅ 每筆交易獨立一行
- ✅ 包含完整銀行信息（名稱、地址）
- ✅ 包含期初和期末餘額
- ✅ 分別顯示 Credits（存入）和 Debits（支出）
- ✅ 顯示每筆交易後的餘額

---

### 2. Xero CSV 格式（圖3）

**適用於**:
- Xero 會計軟件
- 需要簡化的交易記錄

**包含字段**:
```csv
Date,Amount,Payee,Description,Reference,Check Number
```

**示例**:
```csv
02/22/2025,0.00,,B/F BALANCE,,
02/26/2025,2.61,,CREDIT INTEREST,,
03/07/2025,78649.00,,QUICK CHEQUE DEPOSIT(07MAR25),,
03/08/2025,840.00,,POON H** K***HD12530825734031 08MAR,,
```

**特點**:
- ✅ 簡化的字段結構
- ✅ 自動提取 Payee（收款人/付款對象）
- ✅ 兼容 Xero 導入格式
- ✅ 支持 Reference 和 Check Number

**Payee 提取邏輯**:
- 從交易描述中提取常見格式
- 例如: "POON H** K***" → Payee: "POON H** K***"
- 例如: "TUG COMPANY LIMITED" → Payee: "TUG COMPANY LIMITED"

---

### 3. QuickBooks CSV 格式（圖4）

**適用於**:
- QuickBooks 會計軟件
- 需要最簡化的交易記錄

**包含字段**:
```csv
Date,Description,Amount
```

**示例**:
```csv
02/26/2025,CREDIT INTEREST,2.61
03/07/2025,QUICK CHEQUE DEPOSIT(07MAR25),78649.00
03/08/2025,POON H** K***HD12530825734031 08MAR,840.00
```

**特點**:
- ✅ 最精簡的字段結構
- ✅ 僅包含日期、描述、金額
- ✅ 兼容 QuickBooks 導入格式
- ✅ 適合快速記帳

---

## 📁 如何使用

### 步驟 1: 上傳銀行對帳單

1. 前往項目頁面
2. 點擊 "Upload files" 按鈕
3. 選擇文檔類型為 "銀行對帳單"
4. 上傳 PDF 或圖片格式的銀行對帳單
5. 等待 AI 處理完成

### 步驟 2: 選擇要導出的文檔

**方式 1: 導出所有文檔**
- 不勾選任何文檔
- 直接點擊 "Export" 按鈕
- 系統會導出所有已完成的銀行對帳單

**方式 2: 導出特定文檔**
- 勾選要導出的文檔（複選框）
- 點擊 "Export" 按鈕
- 系統只會導出已勾選且已完成的文檔

### 步驟 3: 選擇導出格式

點擊 "Export" 按鈕後，選擇導出格式：

```
┌─────────────────────────────────┐
│ 📥 Export                       │
├─────────────────────────────────┤
│ 📄 標準 CSV                     │
│    通用格式（銀行對帳單）         │
├─────────────────────────────────┤
│ 📄 Xero CSV                     │
│    Xero 會計軟件                 │
├─────────────────────────────────┤
│ 📄 QuickBooks CSV               │
│    QuickBooks 會計軟件           │
├─────────────────────────────────┤
│ ────────────────────────────    │
├─────────────────────────────────┤
│ 📄 IIF                          │
│    QuickBooks Desktop           │
├─────────────────────────────────┤
│ ☁️  QBO                          │
│    QuickBooks Online            │
└─────────────────────────────────┘
```

### 步驟 4: 下載文件

- 系統會自動下載 CSV 文件
- 文件名格式: `BankStatement_YYYYMMDD.csv`
  - 標準 CSV: `BankStatement_20251120.csv`
  - Xero CSV: `BankStatement_20251120_Xero.csv`
  - QuickBooks CSV: `BankStatement_20251120_QuickBooks.csv`

---

## 🔧 技術實現

### 數據提取邏輯

#### 1. 交易類型判斷（Credits/Debits）

```javascript
// 優先級 1: 檢查交易類型字段
if (type.includes('credit') || type.includes('deposit') || type.includes('入')) {
    credits = amount;  // 存入
} else if (type.includes('debit') || type.includes('withdrawal') || type.includes('出')) {
    debits = amount;   // 支出
}

// 優先級 2: 根據金額符號判斷
if (amount >= 0) {
    credits = amount;  // 正數 = 存入
} else {
    debits = Math.abs(amount);  // 負數 = 支出
}
```

#### 2. 日期格式化

所有導出格式統一使用 `MM/DD/YYYY` 格式：

```javascript
function formatDate(dateStr) {
    const date = new Date(dateStr);
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');
    const year = date.getFullYear();
    return `${month}/${day}/${year}`;
}
```

**示例**:
- `2025-02-22` → `02/22/2025`
- `2025-03-07` → `03/07/2025`

#### 3. CSV 字段轉義

處理逗號、引號、換行符：

```javascript
function escapeCSV(value) {
    const str = String(value);
    
    // 如果包含逗號、引號或換行，則用引號包裹
    if (str.includes(',') || str.includes('"') || str.includes('\n')) {
        return `"${str.replace(/"/g, '""')}"`;
    }
    
    return str;
}
```

**示例**:
- `POON H** K***` → `POON H** K***`
- `EAST POINT CITY (766), RM 2505` → `"EAST POINT CITY (766), RM 2505"`
- `Notes: "Important"` → `"Notes: ""Important"""`

#### 4. UTF-8 BOM

確保中文正確顯示：

```javascript
const blob = new Blob(['\uFEFF' + content], { 
    type: 'text/csv;charset=utf-8;' 
});
```

- `\uFEFF` 是 UTF-8 BOM (Byte Order Mark)
- 確保 Excel 和其他軟件正確識別編碼

---

## 📊 數據映射

### 標準 CSV 字段映射

| CSV 字段 | DeepSeek 提取字段 | 備註 |
|----------|-------------------|------|
| CustomerName | accountHolder, account_holder | 帳戶持有人 |
| AccountNumber | accountNumber, account_number | 帳戶號碼 |
| AccountType | accountType | 默認: "Integrated Account" |
| BankName | bankName, bank_name, bank | 銀行名稱 |
| BankAddress | bankAddress, bank_address | 銀行地址 |
| PopulatedDate | statementPeriod（起始日期）| 對帳單開始日期 |
| EndDate | statementPeriod（結束日期）| 對帳單結束日期 |
| OpeningBalance | openingBalance, opening_balance | 期初餘額 |
| EndingBalance | closingBalance, closing_balance, balance | 期末餘額 |
| LineItems_Date | transactions[].date | 交易日期 |
| LineItems_Description | transactions[].description | 交易描述 |
| LineItems_Credits | transactions[].amount（正數）| 存入金額 |
| LineItems_Debits | transactions[].amount（負數）| 支出金額 |
| LineItems_Balance | transactions[].balance | 交易後餘額 |

### Xero CSV 字段映射

| CSV 字段 | DeepSeek 提取字段 | 備註 |
|----------|-------------------|------|
| Date | transactions[].date | 交易日期 |
| Amount | transactions[].amount | 交易金額 |
| Payee | 從 description 提取 | 自動提取收款人 |
| Description | transactions[].description | 交易描述 |
| Reference | transactions[].reference | 參考編號 |
| Check Number | transactions[].checkNumber | 支票號碼 |

### QuickBooks CSV 字段映射

| CSV 字段 | DeepSeek 提取字段 | 備註 |
|----------|-------------------|------|
| Date | transactions[].date | 交易日期 |
| Description | transactions[].description | 交易描述 |
| Amount | transactions[].amount | 交易金額 |

---

## 🧪 測試步驟

### 測試 1: 標準 CSV 導出

1. **上傳測試文檔**:
   ```
   eStatementFile_20250829143359.pdf
   ```

2. **預期結果**:
   ```csv
   CustomerName,AccountNumber,...
   MR YEUNG CAVLIN,766-452064-882,...
   MR YEUNG CAVLIN,766-452064-882,...
   ```

3. **檢查項目**:
   - ✅ 每筆交易獨立一行
   - ✅ Credits 和 Debits 正確分類
   - ✅ 日期格式為 MM/DD/YYYY
   - ✅ 中文正確顯示

### 測試 2: Xero CSV 導出

1. **上傳測試文檔**:
   ```
   eStatementFile_20250829143359.pdf
   ```

2. **預期結果**:
   ```csv
   Date,Amount,Payee,Description,...
   02/22/2025,0.00,,B/F BALANCE,...
   02/26/2025,2.61,,CREDIT INTEREST,...
   ```

3. **檢查項目**:
   - ✅ Payee 正確提取
   - ✅ 日期格式為 MM/DD/YYYY
   - ✅ 字段順序正確

### 測試 3: QuickBooks CSV 導出

1. **上傳測試文檔**:
   ```
   eStatementFile_20250829143359.pdf
   ```

2. **預期結果**:
   ```csv
   Date,Description,Amount
   02/26/2025,CREDIT INTEREST,2.61
   03/07/2025,QUICK CHEQUE DEPOSIT(07MAR25),78649.00
   ```

3. **檢查項目**:
   - ✅ 僅包含 3 個字段
   - ✅ 日期格式為 MM/DD/YYYY
   - ✅ 金額格式正確

---

## 🐛 故障排除

### 問題 1: 導出的 CSV 沒有數據

**可能原因**:
1. 沒有勾選任何已完成的文檔
2. 文檔類型不是銀行對帳單
3. 文檔還在處理中

**解決方案**:
1. 確認文檔狀態為 "已完成"
2. 確認文檔類型為 "銀行對帳單"
3. 檢查 Console 輸出:
   ```javascript
   console.log('📋 導出文檔:', docsToExport);
   ```

### 問題 2: Credits 和 Debits 分類錯誤

**可能原因**:
1. DeepSeek 提取的 `type` 字段不準確
2. 金額符號判斷邏輯問題

**解決方案**:
1. 檢查交易類型判斷邏輯
2. 優先使用 `type` 字段判斷
3. 如果 `type` 不可靠，使用金額符號判斷

### 問題 3: 中文顯示亂碼

**可能原因**:
1. 缺少 UTF-8 BOM
2. Excel 編碼設置問題

**解決方案**:
1. 確認導出時添加了 UTF-8 BOM:
   ```javascript
   const blob = new Blob(['\uFEFF' + content], { 
       type: 'text/csv;charset=utf-8;' 
   });
   ```
2. 在 Excel 中使用 "數據" → "從文本" 導入，選擇 UTF-8 編碼

### 問題 4: 日期格式錯誤

**可能原因**:
1. 日期解析失敗
2. 時區問題

**解決方案**:
1. 檢查日期格式化函數:
   ```javascript
   console.log('原始日期:', dateStr);
   console.log('格式化後:', formatDate(dateStr));
   ```
2. 確認 DeepSeek 提取的日期格式一致

---

## 💡 最佳實踐

### 1. 批量導出

- **建議**: 一次導出多個月的對帳單
- **優點**: 
  - 減少導出次數
  - 數據連續性好
  - 方便財務分析

### 2. 命名規範

導出文件建議命名格式:
```
BankStatement_[銀行名稱]_[帳號]_[期間].csv
```

示例:
```
BankStatement_HangSeng_766452064882_202502_202503.csv
```

### 3. 數據驗證

導出後建議檢查:
- ✅ 期初餘額 = 上月期末餘額
- ✅ 總存入金額 - 總支出金額 = 期末餘額 - 期初餘額
- ✅ 交易筆數與原始對帳單一致

### 4. 備份原始文件

- 保留原始 PDF 文件
- 導出的 CSV 僅用於會計軟件導入
- 如有疑問，可隨時對比原始文件

---

## 📞 需要協助？

如果遇到問題，請：
1. 打開 Chrome DevTools (F12)
2. 切換到 Console 標籤
3. 查看導出日誌:
   ```
   📤 開始導出: csv
   📋 導出 3 個已勾選的文檔
   📊 生成標準銀行對帳單 CSV
   ✅ 導出成功: BankStatement_20251120.csv
   ```
4. 截圖錯誤信息
5. 聯繫技術支援

---

## 🎯 下一步功能

計劃添加的功能:
- [ ] 支持更多會計軟件格式（Sage, MYOB）
- [ ] 支持自定義 CSV 字段
- [ ] 支持批量合併多個對帳單
- [ ] 支持按月份自動分組導出
- [ ] 支持導出統計報表（收入/支出分析）

---

**版本**: v1.0  
**更新日期**: 2025-11-20  
**作者**: VaultCaddy AI Team

