# 🔧 禁用旧 API 函数的修改

## 问题
发现有多个函数仍在使用旧的 `subscriptionItems.createUsageRecord` API，导致与新的 Billing Meter Events 系统冲突。

特别是 `reportDailyUsage` 定时任务每天午夜自动运行，使用旧 API 报告使用量。

---

## 需要禁用的函数

1. **`reportDailyUsage`** (行1876) - 定时任务，每天运行
2. **`reportCreditsUsage`** (行1969) - 手动调用函数  
3. **`reportStripeUsage`** (行1802) - 手动调用函数

---

## 修改方案

### 方案1：在函数开头添加早期返回（推荐，快速）

在每个函数的开头添加：

```javascript
// ❌ 已禁用 - 改用 Billing Meter Events API
console.warn('⚠️ 此函数已禁用，改用 reportUsageToStripe + Billing Meter Events API');
return null; // 或 return { success: false, reason: 'deprecated' };
```

### 方案2：完全注释掉函数（需要手动操作）

---

## 快速修复脚本

我会创建一个简单的修改来禁用这些函数。


