#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
测试v2升级 - 只升级1-2个文件进行测试
"""

import os
import shutil
from pathlib import Path

def main():
    root_dir = Path('/Users/cavlinyeung/ai-bank-parser')
    
    # 选择2个测试文件
    test_files = [
        root_dir / 'chase-bank-statement-v2.html',  # 银行页面
        root_dir / 'restaurant-accounting-v2.html',  # 行业解决方案
    ]
    
    print("🧪 测试v2→v3升级方案")
    print("=" * 80)
    print("\n选择的测试文件:")
    for f in test_files:
        if f.exists():
            print(f"  ✅ {f.name}")
        else:
            print(f"  ❌ {f.name} - 文件不存在")
    
    print("\n建议的升级策略:")
    print("""
    方案A：完整重建 ⭐⭐⭐⭐⭐（推荐）
    - 使用v3模板作为基础
    - 手动或半自动填充v2的内容
    - 质量最高，但需要更多时间
    - 预计时间：每个文件 5-10分钟 × 85 = 7-14小时
    
    方案B：CSS样式升级 ⭐⭐⭐
    - 保留v2的HTML结构
    - 只替换CSS样式为v3
    - 快速，但可能需要后续调整
    - 预计时间：批量处理 30分钟 + 调整 2-3小时
    
    方案C：混合方案 ⭐⭐⭐⭐（平衡）
    - 银行页面使用方案A（重要，质量优先）
    - 其他页面使用方案B（次要，速度优先）
    - 预计时间：50个银行页面 × 5分钟 + 35个其他页面批量 = 4-5小时
    """)

if __name__ == '__main__':
    main()

