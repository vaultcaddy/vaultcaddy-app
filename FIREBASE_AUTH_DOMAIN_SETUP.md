# 🔧 Firebase 認證域名設置指南

**目標**：讓用戶登入時顯示 `vaultcaddy.com` 而不是 `vaultcaddy-production-cbbe2.firebaseapp.com`

---

## 📋 問題說明

**當前問題**：
- 用戶登入時，彈出窗口顯示的是 `vaultcaddy-production-cbbe2.firebaseapp.com`
- 這看起來不專業，用戶可能會疑惑

**期望效果**：
- 用戶登入時，顯示 `vaultcaddy.com`
- 更專業，增加用戶信任度

---

## 🛠️ 解決方案

### 方法：在 Firebase Console 添加授權域名

**步驟非常簡單，只需 3 分鐘！**

---

## 📝 詳細操作步驟

### 步驟 1：打開 Firebase Console

1. ✅ 訪問：https://console.firebase.google.com/
2. ✅ 選擇項目：**vaultcaddy-production-cbbe2**

---

### 步驟 2：進入 Authentication 設置

1. ✅ 在左側菜單點擊 **"Authentication"**（身份驗證）
2. ✅ 點擊頂部的 **"Settings"**（設定）標籤
3. ✅ 向下滾動找到 **"Authorized domains"**（授權網域）部分

---

### 步驟 3：添加授權域名

1. ✅ 點擊 **"Add domain"**（新增網域）按鈕
2. ✅ 輸入：`vaultcaddy.com`
3. ✅ 點擊 **"Add"**（新增）按鈕

**就這麼簡單！** ✅

---

### 步驟 4：驗證配置

**當前應該有的授權域名**：
- ✅ `localhost`（用於本地開發）
- ✅ `vaultcaddy-production-cbbe2.firebaseapp.com`（默認域名）
- ✅ `vaultcaddy-production-cbbe2.web.app`（Firebase Hosting）
- ✅ **`vaultcaddy.com`** ⭐ **新添加的！**

---

## 🧪 測試

### 完成後測試：

1. ✅ 打開 https://vaultcaddy.com
2. ✅ 點擊"登入"按鈕
3. ✅ 使用 Google 登入
4. ✅ **查看彈出窗口的 URL**

**預期結果**：
- 應該顯示 `vaultcaddy.com` 的登入頁面
- 而不是 `vaultcaddy-production-cbbe2.firebaseapp.com`

---

## 🚨 常見問題

### Q1: 添加域名後需要等待嗎？

**A**: 不需要！添加後立即生效，無需等待。

### Q2: 會影響現有用戶嗎？

**A**: 不會！這只是添加一個新的授權域名，不會影響現有功能。

### Q3: 需要修改代碼嗎？

**A**: 不需要！這是純 Firebase Console 設置，無需改代碼。

### Q4: 如果我有子域名怎麼辦？

**A**: 如果您使用子域名（如 `app.vaultcaddy.com`），也需要添加該子域名。

---

## 📸 截圖參考

### 找到 "Authorized domains" 位置：

```
Firebase Console
└── Authentication（身份驗證）
    └── Settings（設定）
        └── Authorized domains（授權網域）⭐
            ├── localhost
            ├── vaultcaddy-production-cbbe2.firebaseapp.com
            ├── vaultcaddy-production-cbbe2.web.app
            └── ➕ Add domain（新增網域）
```

---

## ✅ 完成檢查清單

- [ ] 訪問 Firebase Console
- [ ] 進入 Authentication > Settings
- [ ] 找到 Authorized domains 部分
- [ ] 點擊 "Add domain"
- [ ] 輸入 `vaultcaddy.com`
- [ ] 點擊 "Add" 保存
- [ ] 測試登入流程

---

## 🎉 完成後

**恭喜！** 現在用戶登入時會看到更專業的域名了！

**下一步**：繼續實現首頁文件上傳功能 🚀

---

## 💡 額外提示

### 如果您使用自定義域名

如果您已經將 Firebase Hosting 連接到 `vaultcaddy.com`：

1. ✅ 確保 DNS 記錄正確配置
2. ✅ 確保 SSL 證書已啟用
3. ✅ 在 Firebase Hosting 設置中驗證自定義域名

### 如果您使用多個域名

例如：
- `vaultcaddy.com`（主域名）
- `www.vaultcaddy.com`（www 子域名）
- `app.vaultcaddy.com`（應用子域名）

**每個域名都需要添加到授權域名列表中！**

---

**預計完成時間**：3-5 分鐘  
**難度等級**：⭐ 非常簡單

---

**需要幫助？**
- Firebase 官方文檔：https://firebase.google.com/docs/auth/web/google-signin
- 授權域名說明：https://firebase.google.com/docs/auth/web/redirect-best-practices

---

**文檔版本**: 1.0.0  
**創建日期**: 2026-01-31  
**維護者**: VaultCaddy Development Team

